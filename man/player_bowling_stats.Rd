% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/player_metrics.R
\name{player_bowling_stats}
\alias{player_bowling_stats}
\alias{calculate_player_bowling_stats}
\title{Player Bowling Stats}
\usage{
player_bowling_stats(
  player_id = NULL,
  match_type = NULL,
  season = NULL,
  min_balls = 100,
  source = c("local", "remote"),
  db_path = NULL
)

calculate_player_bowling_stats(
  player_id = NULL,
  match_type = NULL,
  season = NULL,
  min_balls = 100,
  source = c("local", "remote"),
  db_path = NULL
)
}
\arguments{
\item{player_id}{Character. Player identifier (e.g., "JJ Bumrah", "Rashid Khan").
If NULL (default), returns stats for all players.}

\item{match_type}{Character. Filter by match type: "T20", "ODI", "Test",
"IT20", "MDM", or NULL for all formats.}

\item{season}{Character. Filter by season (e.g., "2023", "2023/24").}

\item{min_balls}{Integer. Minimum balls bowled to include (default 100).
Only applies when player_id is NULL.}

\item{source}{Character. "local" (default) uses local DuckDB for fast SQL
aggregation. "remote" loads data from GitHub releases and aggregates in R.}

\item{db_path}{Character. Database path (only used when source = "local").}
}
\value{
Data frame with bowling metrics:
\itemize{
\item bowler_id, player_name, balls_bowled, runs_conceded, wickets
\item bowling_average, economy_rate, strike_rate, wicket_pct
}
}
\description{
Get bowling statistics for one or all players. Includes average, economy rate,
strike rate, and derived metrics.
}
\examples{
\dontrun{
# Get all players' bowling stats
all_bowlers <- player_bowling_stats()

# Get stats for a specific player
bumrah_stats <- player_bowling_stats("JJ Bumrah")

# Get T20 bowling stats for all players
t20_bowlers <- player_bowling_stats(match_type = "T20")

# Get Test bowling stats for a specific player
anderson_tests <- player_bowling_stats("JM Anderson", match_type = "Test")

# Get stats from GitHub releases (no local install needed)
remote_stats <- player_bowling_stats(match_type = "T20", source = "remote")
}
}
