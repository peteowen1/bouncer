% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cricsheet_parser.R
\name{compute_is_free_hit}
\alias{compute_is_free_hit}
\title{Derive Free-Hit Status Per Delivery}
\usage{
compute_is_free_hit(d)
}
\arguments{
\item{d}{data.table (or data.frame) with \code{match_id}, \code{innings}, \code{over},
\code{ball}, \code{wides}, \code{noballs}, already in true bowling order (cricsheet
numbers \code{ball} by delivery sequence within an over, including illegal
deliveries, so sorting by \verb{(match_id, innings, over, ball)} is bowling
order -- see the \code{over_ball} note in \code{CLAUDE.md}/D-P5).}
}
\value{
Logical vector, one value per row of \code{d}, in \code{d}'s own row order
(NOT resorted -- safe to assign straight back onto the input frame).
}
\description{
Cricsheet's JSON has no \code{free_hit} field (checked against the published
schema and a real no-ball delivery, bouncerverse#81/D-P50) -- it has to be
derived from what IS stored. A no-ball (not a wide) triggers a free hit on
the next delivery; if that delivery is itself illegal (wide or no-ball),
the free hit carries forward again until a legal delivery is actually
bowled and faced (ICC playing conditions).
}
\section{Why this vectorizes without a loop}{

A legal delivery always exits with pending = FALSE (nothing carries past
it). So within one uninterrupted run of illegal deliveries bounded by
legal ones, the exit-pending value is just \code{cummax()} of "is this row a
no-ball" from the start of that run -- \code{rleid()} on the illegal flag
groups exactly those runs. \code{is_free_hit} for a row is the value CARRIED
IN, i.e. the previous row's exit-pending, hence the trailing \code{shift()}.
}

\keyword{internal}
