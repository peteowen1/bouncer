% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_projection_stage2.R
\name{calculate_test_projected_scores_v2}
\alias{calculate_test_projected_scores_v2}
\title{Project a Test/MDM innings with the two-stage model}
\usage{
calculate_test_projected_scores_v2(
  match_type,
  current_score,
  wickets_before,
  wickets_remaining,
  balls_before,
  run_rate,
  lead,
  innings,
  match_balls_before,
  eis,
  overs_models = NULL,
  stage2_corrections = NULL
)
}
\arguments{
\item{wickets_before}{Integer/numeric vector. Wickets already lost BEFORE
this ball -- the state used to look up the overs model and stage-2
correction (both were fit on this pre-ball quantity).}

\item{wickets_remaining}{Numeric vector. Wickets remaining to use in the
resource formula -- differs from \code{10 - wickets_before} whenever this
ball's own outcome matters, which is exactly the case TSA needs: the
"actual" branch passes \code{10 - wickets_before - is_wicket}, the "expected"
branch passes \code{10 - wickets_before - exp_wicket}. Collapsing these two
into one WAS a real bug here (2026-09-04): computing wr internally from
wickets_before alone made both TSA branches share identical resource,
which zeroed out the wicket signal (wicket cost ended up barely
different from a dot ball) -- caught by the SAME per-ball anchor check
this function's callers already run before persisting.}

\item{balls_before, run_rate, lead, innings, match_balls_before, eis, current_score}{Numeric/integer vectors. Same convention as calculate_test_projected_scores().}

\item{overs_models}{Named list keyed by match_type, each a
fit_test_overs_model() (or load_test_overs_model()) result. NULL loads
both from disk.}

\item{stage2_corrections}{Named list keyed by match_type, each a
fit_test_stage2_correction() (or load_test_stage2_correction()) result.
NULL loads both from disk.}
}
\value{
Numeric vector: projected final innings score.
}
\description{
Replaces calculate_test_projected_scores()'s single-stage call. Same
inputs; stage1 (bounded, current_score + a\emph{EIS}resource_remaining) feeds a
fitted stage2 correction instead of the old multiplicative current-rate
term.
}
\keyword{internal}
