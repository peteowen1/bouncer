% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cricinfo_win_probability.R
\name{build_cricinfo_win_probability}
\alias{build_cricinfo_win_probability}
\title{Build Bouncer Win Probability for Every Cricinfo Delivery}
\usage{
build_cricinfo_win_probability(
  format = c("t20", "odi"),
  conn = NULL,
  models_path = NULL,
  write = TRUE,
  table_name = "cricinfo_ball_win_probability"
)
}
\arguments{
\item{format}{Character. "t20" or "odi". Test is not supported -- it runs
through the decomposed \code{predict_test_win_probability()}, which
\code{\link[=predict_win_probability_batch]{predict_win_probability_batch()}} rejects rather than mishandles.}

\item{conn}{DBI connection. If NULL, opens one (write access when
\code{write = TRUE}) and closes it on exit.}

\item{models_path}{Character. Directory holding the in-match models. NULL
resolves via \code{\link[=load_in_match_models]{load_in_match_models()}}, which derives it from the database
path -- pass it explicitly when running against a dev checkout whose
models are not in the user data directory.}

\item{write}{Logical. Write the table, or just return the scored frame.}

\item{table_name}{Character. Target table in the \code{main} schema.}
}
\value{
data.table with \code{id}, \code{match_id}, \code{innings_number}, \code{over_number},
\code{ball_number}, \code{format} and \code{win_probability} (P(batting-first team wins),
0-1), invisibly when \code{write = TRUE}.
}
\description{
Scores every delivery of a limited-overs format from \code{cricinfo.balls} with
bouncer's own in-match models and writes the result to
\code{main.cricinfo_ball_win_probability}.
}
\section{Why the join key is \code{id}}{

\verb{(match_id, innings_number, over_number, ball_number)} is \strong{not} unique in
\code{cricinfo.balls}: 546,034 T20/ODI rows collapse to 546,028 distinct
composites, six of them duplicated in match \code{1099000} innings 1 over 30.
Joining on the composite would silently multiply those rows in any
downstream aggregate. \code{id} is unique across all 546,034 and is what this
table keys on.
}

\section{Momentum comes from the training helper, zero-fill included}{

The 14 momentum features are computed with \code{\link[=calculate_rolling_features]{calculate_rolling_features()}} --
the same function \verb{data-raw/models/in-match/01_prepare_all_formats.R} uses to
build the training set. That function zero-fills the incomplete windows at
the start of an innings (\code{feature_engineering.R:93-97}), which in isolation
is the exact hazard that broke the serving path: zero means "no runs and no
wickets in the last N balls", a real and extreme state.

It is nonetheless correct to keep it here, because the models were \emph{trained}
on those zero-filled early-innings rows. Reproducing training's treatment is
what makes train and serve agree; substituting the scalar path's run-rate
imputation would introduce a skew rather than remove one. Note that this
means \code{\link[=predict_win_probability]{predict_win_probability()}}'s no-history fallback and this path
deliberately differ for the first 24 balls of an innings.
}

