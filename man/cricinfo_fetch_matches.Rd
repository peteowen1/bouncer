% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cricinfo_scraper.R
\name{cricinfo_fetch_matches}
\alias{cricinfo_fetch_matches}
\title{Fetch Multiple Cricinfo Matches with Rate Limiting}
\usage{
cricinfo_fetch_matches(
  browser,
  series_id,
  match_ids,
  output_dir = "../bouncerdata/cricinfo",
  format_subdir = NULL,
  include_commentary = FALSE,
  include_scorecard = TRUE,
  skip_existing = TRUE,
  delay_between = 8,
  max_consecutive_failures = 5
)
}
\arguments{
\item{browser}{A ChromoteSession object with Network enabled}

\item{series_id}{Integer series objectId}

\item{match_ids}{Integer vector of match objectIds}

\item{output_dir}{Base directory to save match files (default: bouncerdata/cricinfo)}

\item{format_subdir}{Subdirectory name for format (e.g., "t20i", "test")}

\item{include_commentary}{Fetch ball-by-ball commentary via scroll capture
(default FALSE). Set to TRUE for matches where you want pitch/shot/wagon data.}

\item{include_scorecard}{Fetch scorecard data (default TRUE)}

\item{skip_existing}{Skip completed matches that already have saved details}

\item{delay_between}{Seconds to wait between matches (default 8)}

\item{max_consecutive_failures}{Stop and reconnect after this many failures (default 5)}
}
\value{
data.frame of all combined commentary data, or NULL
}
\description{
Downloads data for multiple matches from a series with automatic
rate limiting and browser reconnection on failure.
}
\examples{
\dontrun{
browser <- chromote::ChromoteSession$new()
browser$Network$enable()
cricinfo_apply_stealth(browser)

match_ids <- c(1473561, 1473562, 1473563)
data <- cricinfo_fetch_matches(browser, series_id = 1455609,
                               match_ids = match_ids,
                               format_subdir = "t20i")
}

}
