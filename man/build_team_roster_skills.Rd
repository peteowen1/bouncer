% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_inputs.R
\name{build_team_roster_skills}
\alias{build_team_roster_skills}
\title{Build One Team's Batter/Bowler Skill Lists}
\usage{
build_team_roster_skills(roster, format, conn, id_map = NULL)
}
\arguments{
\item{roster}{data.table with \code{player_id}, \code{player_name} (one row per
squad/roster member; \code{player_id} may be \code{NA} for an unresolved name).}

\item{format}{Character. Normalized format.}

\item{conn}{DBI connection.}

\item{id_map}{Optional canonical-id map from \code{\link[=build_player_id_map]{build_player_id_map()}}.
\code{main.match_squads} ids are name-keyed for ~4-5\% of rows (bouncerverse#74)
and will silently miss the skill-table join without this; pass a
pre-built map when assembling many fixtures (a season/tournament) to
amortise its one expensive full-table scan, and skip it for a single
ad hoc match.}
}
\value{
List with \code{batters}, \code{bowlers} (each a list of per-player skill
lists, length \code{nrow(roster)}), \code{n_players}, \code{n_batters_resolved},
\code{n_bowlers_resolved}, \code{unresolved_players} (names with neither role
resolved).
}
\description{
The shared roster-building step behind \code{\link[=build_match_simulation_inputs]{build_match_simulation_inputs()}}:
takes a set of (player_id, player_name) pairs -- however they were
sourced -- resolves skills for all of them in two batched queries, and
returns the shapes \code{\link[=simulate_match_ballbyball]{simulate_match_ballbyball()}} reads.
}
\details{
Batters are returned in roster order (registry/squad order -- neither
\code{main.match_squads} nor a caller's name vector carries a real batting
order, so this is the best available and callers who want a specific
order should reorder before passing it on). Bowlers are the SAME roster,
reordered so players with a resolved bowling-skill row sort first: there
is no role column anywhere upstream (\code{main.match_squads} lists an XI, not
batters vs. bowlers), so "has bowled a ball in this format before" is the
only real signal available for who should get the overs.
}
\keyword{internal}
