% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raa.R
\name{build_cricinfo_raa}
\alias{build_cricinfo_raa}
\title{Build Runs Above Average for Every Cricinfo Delivery}
\usage{
build_cricinfo_raa(
  format = c("t20", "odi", "test"),
  conn = NULL,
  model = NULL,
  write = TRUE,
  table_name = "cricinfo_ball_raa"
)
}
\arguments{
\item{format}{Character. "t20" for now; other formats need their lambda
fitted first (\code{\link[=get_raa_lambda]{get_raa_lambda()}}).}

\item{conn}{DBI connection. If NULL, opens one (write access when
\code{write = TRUE}) and closes it on exit.}

\item{model}{Optional pre-loaded agnostic model from
\code{\link[=load_agnostic_model]{load_agnostic_model()}}; loaded if NULL.}

\item{write}{Logical. Write the table, or just return the scored frame.}

\item{table_name}{Character. Target table in the \code{main} schema.}
}
\value{
data.table with one row per scored delivery: \code{id}, \code{match_id},
\code{innings_number}, \code{over_number}, \code{ball_number}, \code{format},
\code{batsman_player_id}, \code{exp_runs}, \code{exp_wicket}, \code{actual_runs},
\code{is_wicket}, \code{raa_run}, \code{raa_wicket}, \code{raa}. Invisibly when
\code{write = TRUE}.
}
\description{
Scores every batter-faced delivery of a format from \code{cricinfo.balls} with
the agnostic outcome model and writes per-ball RAA to
\code{main.cricinfo_ball_raa}, keyed on \code{cricinfo.balls.id} (the composite
(match, innings, over, ball) is not unique -- see
\code{\link[=build_cricinfo_win_probability]{build_cricinfo_win_probability()}}).
}
\details{
Wides are excluded: the batter does not face a wide, and the batting
aggregation in \code{player_game_data.R} excludes them for every other batting
stat. No-balls are faced and are included. Deliveries where the batter ran
5 or 7 are scored as-is even though the outcome model's classes stop at 6;
they are ~1 in 7,000 balls and their expectation is still state-correct.
}
