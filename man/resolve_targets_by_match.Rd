% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/in_match_prediction.R
\name{resolve_targets_by_match}
\alias{resolve_targets_by_match}
\title{Calculate Win Probability for All Deliveries in a Match}
\usage{
resolve_targets_by_match(deliveries, target = NULL)
}
\arguments{
\item{deliveries}{data.frame with at least \code{match_id}, \code{innings} and
\code{total_runs} (cumulative). May span many matches.}

\item{target}{NULL to derive a target per match from its own first innings,
or a single value to apply to every match in the frame.}

\item{format}{Character. Match format.}

\item{models}{List. Pre-loaded models (optional).}

\item{wpa_failure_threshold}{Numeric in [0, 1]. Abort if this proportion
of deliveries or more fail to produce a win probability. Default 0.01.
Set to 1 to warn but never abort.}
}
\value{
data.frame with additional columns:
\itemize{
\item win_prob_before - Win probability before this delivery
\item win_prob_after - Win probability after this delivery
\item wpa - Win Probability Added (change from this delivery)
}

Resolve the Chase Target for Each Match in a Delivery Frame

Pulled out of \code{\link[=add_win_probability]{add_win_probability()}} so the per-match target rule can be
tested without any trained model present. That matters because CI checks
out only this repo — \verb{bouncerdata/models/} is absent, so every test that
needs a real in-match model skips, and the target logic would otherwise
never be re-verified by a green CI run.

Named numeric vector of targets, keyed by \code{match_id}. Matches with
no first-innings rows are absent from it; callers must treat a missing
name as "unknown target", not as zero.
}
\description{
Adds win probability columns to a data frame of deliveries.
}
\keyword{internal}
