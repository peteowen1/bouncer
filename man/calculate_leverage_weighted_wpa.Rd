% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leverage.R
\name{calculate_leverage_weighted_wpa}
\alias{calculate_leverage_weighted_wpa}
\title{Calculate Leverage-Weighted WPA per Player-Match}
\usage{
calculate_leverage_weighted_wpa(format = c("t20", "odi"), conn = NULL)
}
\arguments{
\item{format}{Character. "t20" or "odi".}

\item{conn}{DBI connection. If NULL, opens a read-only one and closes it on
exit.}
}
\value{
data.table with \code{match_id}, \code{player_id}, \code{role}, \code{deliveries},
\code{total_wpa}, \code{total_leverage_weighted_wpa}, \code{mean_leverage}.
}
\description{
Joins \code{main.bouncer_wp_from_cricinfo} and \code{main.bouncer_leverage_from_cricinfo}
to \code{cricinfo.balls}, computes the batting-team-signed per-ball WPA (same
team-sign construction as \code{.wp_source_sql()} in \code{player_game_data.R} --
replicated here rather than shared because one lives in SQL and this in R;
see that function if the two ever need to be reconciled), multiplies by
leverage, credits batter/bowler via the existing \code{\link[=assign_delivery_credit]{assign_delivery_credit()}}
(unchanged from plain WPA's credit logic -- wides/no-balls to the bowler
only, byes/leg-byes neutral), and aggregates to one row per player per
match per role.
}
\details{
This is a standalone diagnostic, deliberately NOT wired into
\code{calculate_impact()} or \code{player_game_data.R}'s live rating composite --
MODELLING-IDEAS.md's "plug in as a variant, not a replacement".
}
