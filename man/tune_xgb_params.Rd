% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xgb_tuning.R
\name{tune_xgb_params}
\alias{tune_xgb_params}
\title{Tune XGBoost Hyperparameters via Random Search}
\usage{
tune_xgb_params(
  dtrain,
  folds,
  fixed_params = list(objective = "multi:softprob", num_class = 7, eval_metric =
    "mlogloss"),
  n_iter = 20,
  max_rounds = 2000,
  early_stopping = 20,
  seed = 42,
  param_grid = NULL,
  verbose = TRUE
)
}
\arguments{
\item{dtrain}{xgb.DMatrix. Training data}

\item{folds}{List of integer vectors. Grouped CV fold indices
(e.g., from match-grouped splitting)}

\item{fixed_params}{List. Fixed XGBoost parameters (objective, eval_metric, num_class)}

\item{n_iter}{Integer. Number of random parameter combinations to try (default 20)}

\item{max_rounds}{Integer. Maximum boosting rounds per trial (default 2000)}

\item{early_stopping}{Integer. Early stopping patience (default 20)}

\item{seed}{Integer. Random seed for reproducibility}

\item{param_grid}{Named list. Parameter search spaces. Each element is a
list with 'min' and 'max' for continuous params or a vector for discrete.
Defaults to a sensible grid for tree-based models.}

\item{verbose}{Logical. Print progress (default TRUE)}
}
\value{
Named list with:
\itemize{
\item best_params: the best parameter combination found
\item best_score: the best CV score
\item best_nrounds: optimal number of rounds for best params
\item all_results: data.frame of all trial results
\item tuning_time_mins: total tuning duration in minutes
}
}
\description{
Performs random search over XGBoost hyperparameters using match-grouped
cross-validation folds. Returns the best parameter set found.
}
