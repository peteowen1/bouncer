% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/in_match_prediction.R
\name{predict_win_probability}
\alias{predict_win_probability}
\title{Predict Win Probability from Current Game State}
\usage{
predict_win_probability(
  current_score,
  wickets,
  overs,
  innings,
  target = NULL,
  format = "t20",
  venue_stats = NULL,
  match_state = NULL,
  skill_adjustments = NULL,
  models = NULL,
  recent_balls = NULL
)
}
\arguments{
\item{current_score}{Integer. Current team score.}

\item{wickets}{Integer. Wickets fallen (0-10).}

\item{overs}{Numeric. Overs bowled in cricket notation (e.g., 10.3 = 10 overs + 3 balls).}

\item{innings}{Integer. Current innings (1-2 for limited overs, 1-4 for Test).}

\item{target}{Integer. Target score (required if innings = 2 for limited overs,
or innings = 4 for Test).}

\item{format}{Character. Match format: "t20", "odi", "test".}

\item{venue_stats}{List. Venue-specific statistics (optional).
If NULL, uses format averages. For Test, can include venue_avg (1st innings
average) and venue_result_rate (historical P(result) at venue).}

\item{match_state}{List. Additional Test match state (optional). Can include:
\itemize{
\item completed_innings - list of lists with runs, wickets, overs per innings
\item batting_is_team1 - logical, is batting team the team listed first?
}}

\item{skill_adjustments}{List. Team/player skill adjustments (optional).}

\item{models}{List. Pre-loaded models from load_in_match_models().
If NULL, models are loaded automatically.}

\item{recent_balls}{Named list of momentum features for the last few overs
(\code{runs_last_12_balls}, \code{dots_last_24_balls}, \code{rr_last_6_overs}, ...) as
produced by \code{\link[=calculate_rolling_features]{calculate_rolling_features()}} over the delivery sequence. The
trained models use 14 of these. A scoreboard state cannot supply them, so
when this is NULL they are imputed from the current run rate and a warning
is emitted. They are never zero-filled -- zero means "no runs and no
wickets in the last N balls", an extreme real state, and substituting it is
what made the chase model return ~0.9 regardless of situation.}
}
\value{
A \code{bouncer_win_prob} object. For limited-overs formats:
\itemize{
\item win_prob - Win probability for batting-first team (0-1)
\item projected_score - Projected final innings score
}
For Test format, additionally:
\itemize{
\item draw_prob - Draw probability (0-1)
\item team1_win - Team 1 win probability
\item team2_win - Team 2 win probability
}
}
\description{
Calculate win probability for the batting-first team given the current
match state. Uses scoreboard-friendly inputs. For Test matches, returns
three-way probabilities (team1 win, draw, team2 win) via a decomposed
two-model pipeline.
}
\section{This model DOES feed the player ratings (superseded 2026-08-13)}{

This docstring said the opposite until 2026-08-29 — stale from the day
after it was written. D-P6 (\code{docs/DECISIONS.md}) resolved in favour of
wiring this model family in: \code{\link[=build_cricinfo_win_probability]{build_cricinfo_win_probability()}} calls
\code{\link[=load_in_match_models]{load_in_match_models()}} directly and scores every Cricinfo T20/ODI
delivery into \code{main.bouncer_wp_from_cricinfo}, which
\code{player_game_data.R} joins (\code{wp_source = "bouncer"}, the default) to
produce \code{batting_wpa}/\code{bowling_wpa} — the inputs to \code{calculate_impact()}'s
\code{raa + kappa*wpa} composite, which feeds
\code{\link[=calculate_epr]{calculate_epr()}}/\code{\link[=calculate_bouncer]{calculate_bouncer()}}. The older scraped
\code{cricinfo.balls.win_probability} column is still selectable via
\code{wp_source = "cricinfo"} for comparison only (see \code{bouncer/CLAUDE.md}'s WPA
section for the coverage/Brier comparison). The similarly-named
\code{\link[=build_cricsheet_win_probability]{build_cricsheet_win_probability()}} (\code{main.bouncer_wp_from_cricsheet})
is a separate validation/comparison artifact over the deeper Cricsheet
source — not what \code{player_game_data.R} reads. Improving THIS model family
(innings1/stage2) now does flow through to the ratings.

\code{plot_win_probability()} (this function called directly) is a separate,
unrelated caller used only for live-match charts.
}

\examples{
\dontrun{
# T20 first innings: India 85/2 after 10 overs
wp <- predict_win_probability(85, 2, 10.0, innings = 1, format = "t20")
print(wp)

# T20 second innings: Chasing 180, currently 100/3 after 12.4 overs
wp <- predict_win_probability(100, 3, 12.4, innings = 2, target = 180, format = "t20")
print(wp)

# Test match: 3rd innings, team2 batting at 150/4 after 50 overs
wp <- predict_win_probability(150, 4, 50, innings = 3, format = "test",
  match_state = list(
    completed_innings = list(
      list(runs = 350, wickets = 10, overs = 120),
      list(runs = 280, wickets = 10, overs = 95)
    ),
    batting_is_team1 = FALSE
  ))
print(wp)
}
}
