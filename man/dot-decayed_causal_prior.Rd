% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/venue_rates.R
\name{.decayed_causal_prior}
\alias{.decayed_causal_prior}
\title{Decayed causal prior n/v for one grouping level}
\usage{
.decayed_causal_prior(daily_n, daily_v, days, half_life)
}
\arguments{
\item{daily_n, daily_v}{Numeric vectors, one per (group, date) row, already
sorted by date ascending WITHIN the group this is called on.}

\item{days}{Numeric vector of that row's \code{match_date} as a day count
(e.g. \code{as.numeric(match_date)}), same order as \code{daily_n}/\code{daily_v}.}

\item{half_life}{Numeric, in days. \code{Inf} = no decay.}
}
\value{
list(n_prior, v_prior), same length and order as the inputs.
}
\description{
The recursive step \code{\link[=time_causal_hierarchical_mean_decayed]{time_causal_hierarchical_mean_decayed()}} needs at each
level: given one row per (group, date) with that date's own total \code{.has}/
\code{.v} (same-day siblings already pooled), compute the EXPONENTIALLY DECAYED
sum of every STRICTLY EARLIER date in the same group, weighted by
\code{exp(-days_since/half_life)}.
}
\details{
Computed as a single forward pass per group (dates already sorted
ascending): carry \code{T}, the decayed cumulative total AS OF AND INCLUDING the
current date, forward as \verb{T_i = daily_i + exp(-gap/half_life) * T_\{i-1\}}.
The PRIOR value at date i (excluding date i itself) is then
\verb{exp(-gap/half_life) * T_\{i-1\}} -- one date's lag on the same recursion,
not a second computation. \code{half_life = Inf} degenerates to the undecayed
flat sum (\code{exp(-gap/Inf) == 1} for every finite gap), so this is a proper
superset of the undecayed behaviour, not a parallel implementation of it.
}
\keyword{internal}
