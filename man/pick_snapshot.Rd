% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/team_rating.R
\name{pick_snapshot}
\alias{pick_snapshot}
\title{Pick the Rating Snapshot That Could Have Been Known Before a Match}
\usage{
pick_snapshot(match_date, snapshot_dates)
}
\arguments{
\item{match_date}{Date vector.}

\item{snapshot_dates}{Date vector of available snapshots.}
}
\value{
Date vector, \code{NA} where no snapshot precedes the match.
}
\description{
\code{calculate_player_rating_v2(as_at = D)} filters \code{match_date <= D}, so a
snapshot dated D contains matches played ON D. Scoring a match on D with
that snapshot leaks it. This returns the latest snapshot STRICTLY BEFORE
each match date, or \code{NA} where none exists.
}
\details{
Written as its own function with its own tests because the off-by-one here
is invisible in output: a leaked team rating does not look wrong, it looks
good (bouncerverse#61, and #29/#69 for the same shape).
}
\keyword{internal}
