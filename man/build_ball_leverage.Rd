% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leverage.R
\name{build_ball_leverage}
\alias{build_ball_leverage}
\title{Build Leverage for Every Cricinfo Delivery}
\usage{
build_ball_leverage(
  format = c("t20", "odi"),
  conn = NULL,
  models_path = NULL,
  write = TRUE,
  table_name = "bouncer_leverage_from_cricinfo"
)
}
\arguments{
\item{format}{Character. "t20" or "odi". Test is not supported -- see
\code{\link[=build_cricinfo_test_win_probability]{build_cricinfo_test_win_probability()}}'s own decomposed pipeline; a
single win-probability delta doesn't apply the same way across four
innings and three outcomes, and leverage over that surface is a
separate, harder question left for a later pass.}

\item{conn}{DBI connection. If NULL, opens one (write access when
\code{write = TRUE}) and closes it on exit.}

\item{models_path}{Character. Directory holding the in-match models; NULL
resolves via \code{\link[=load_in_match_models]{load_in_match_models()}}.}

\item{write}{Logical. Write the table, or just return the scored frame.}

\item{table_name}{Character. Target table in the \code{main} schema.}
}
\value{
data.table with \code{id}, \code{match_id}, \code{leverage}, \code{p_wicket}, \code{p_six}
(the two most-referenced individual outcome probabilities, kept for
diagnostics). Invisibly when \code{write = TRUE}.
}
\description{
Scores every T20/ODI delivery in \code{cricinfo.balls} with leverage(state) =
Var\link{WP(state + outcome)}, weighted by P(outcome | state) from the agnostic
ball-outcome model, over the seven scoreable outcomes in
\link{.LEVERAGE_OUTCOMES}. Writes \code{main.bouncer_leverage_from_cricinfo}, joinable to
\code{main.bouncer_wp_from_cricinfo} on \code{id}.
}
\details{
Reuses \code{\link[=build_cricinfo_win_probability]{build_cricinfo_win_probability()}}'s pre-delivery state construction
(momentum, venue stats, target derivation, gap handling) via
\code{return_pre_states = TRUE} rather than a second, drifting copy of it.
}
