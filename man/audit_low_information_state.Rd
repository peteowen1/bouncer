% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration_audit.R
\name{audit_low_information_state}
\alias{audit_low_information_state}
\title{Check a low-information state for the leak's signature}
\usage{
audit_low_information_state(
  predicted,
  actual,
  state,
  min_n = 30,
  correlation_threshold = 0.2
)
}
\arguments{
\item{predicted}{Numeric vector of model predictions, over the FULL data
set (not pre-filtered) -- \code{state} does the filtering.}

\item{actual}{Numeric vector of realised outcomes, same length.}

\item{state}{Logical vector, same length, \code{TRUE} for rows in the
low-information state to check.}

\item{min_n}{Minimum number of matching rows to judge (default 30).}

\item{correlation_threshold}{Absolute correlation between \code{predicted} and
\code{actual} at or above which the state is flagged \code{"leak_signature"}
(default 0.2). A model with no information at this state should show
close to zero correlation with that same row's outcome; the leaked
model showed 1.000.}
}
\value{
A one-row data frame (class \code{low_information_audit}) with \code{n},
\code{judged}, \code{min_prediction}, \code{max_prediction}, \code{mean_prediction},
\code{sd_prediction}, \code{correlation_with_outcome}, and \code{flag}
(\code{"ok"}/\code{"leak_signature"}/\code{"not_judged"}).
}
\description{
Every innings opens 0/0: a model with no player identity should predict
nearly the same value for every first ball, because it has no information
to distinguish them. The leaked agnostic model predicted 0.005 to 5.499
runs for that exact state and correlated 1.000 with the runs actually
scored on the ball being predicted -- the model was looking at its own
answer. This function makes that check reusable for any state the caller
can nominate as "the model should not know much here" (first ball of an
innings, first over of a powerplay, a brand-new venue, etc).
}
