% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_inputs.R
\name{batch_resolve_player_skills}
\alias{batch_resolve_player_skills}
\title{Batch-Resolve Player Skills From the Format Skill Table}
\usage{
batch_resolve_player_skills(player_ids, format, conn)
}
\arguments{
\item{player_ids}{Character vector. Player ids to resolve (may contain
\code{NA}, which is dropped before querying).}

\item{format}{Character. Normalized format ("t20", "odi", "test").}

\item{conn}{DBI connection.}
}
\value{
data.table keyed by \code{player_id} with \code{batter_scoring_index},
\code{batter_survival_rate}, \code{bowler_economy_index}, \code{bowler_strike_rate},
\code{resolved_batter}, \code{resolved_bowler} (logical). One row per distinct
non-NA input id; ids never seen in either role still get a row with both
\verb{resolved_*} FALSE, so the caller can join without losing anyone.
}
\description{
Looks up the latest batting-role and bowling-role skill row for each
player id, in two separate queries. This is deliberately NOT the same
query \code{get_player()} runs (\verb{WHERE batter_id = ? OR bowler_id = ?} pulling
all four skill columns from whichever row matched): that table is keyed
per delivery, one row per ball, and a row where a player appears as
\code{batter_id} carries the \emph{opposing bowler's} \code{bowler_economy_index} /
\code{bowler_strike_rate} in the same row, not the player's own bowling skill.
Pulling all four columns off one row silently mixes the two players. This
function only ever reads \verb{batter_*} columns from a row where the player
was the batter, and \verb{bowler_*} columns from a row where the player was the
bowler, so the two roles never cross-contaminate.
}
\keyword{internal}
