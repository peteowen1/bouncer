% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_inputs.R
\name{resolve_named_players}
\alias{resolve_named_players}
\title{Resolve Caller-Supplied Player Names to Ids}
\usage{
resolve_named_players(names, conn)
}
\arguments{
\item{names}{Character vector of player names.}

\item{conn}{DBI connection.}
}
\value{
data.table of \code{player_name} (the input, unchanged) and \code{player_id}
(\code{NA} if nothing matched).
}
\description{
Used for the hypothetical-fixture path, where there is no squad row to
read a player_id from. Deliberately simpler than \code{\link[=find_player]{find_player()}}: this
only needs an id, not a career-ball-volume ranking, and skipping that
avoids joining the full deliveries table per player for what is usually a
handful of names. Falls through exact match -> case-insensitive exact ->
substring, same tiering rationale as \code{find_player()} / \code{get_player()}
(an ambiguous or absent match must not silently pick a false positive).
}
\keyword{internal}
