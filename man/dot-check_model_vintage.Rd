% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agnostic_model.R
\name{.check_model_vintage}
\alias{.check_model_vintage}
\title{Refuse an outcome model that predates the leak fix}
\usage{
.check_model_vintage(model, model_name, source)
}
\arguments{
\item{model}{Loaded xgb.Booster}

\item{model_name}{Character, for the error message}

\item{source}{Character, where it came from, for the error message}
}
\value{
\code{model}, invisibly, when it passes
}
\description{
Models carry their build date as the xgboost attribute
\code{bouncer_build_date}, stamped at save time by the trainers in
\verb{data-raw/models/ball-outcome/}. An \strong{unstamped} model is refused too: every
artefact built before the stamp existed also predates the fix, so "no stamp"
and "stamped too early" mean the same thing.
}
\details{
This exists because \code{load_agnostic_model()} and \code{load_full_model()} prefer the
\code{bouncermodels} release over local disk, and the release served a 2026-03-27
vintage — so any machine with \code{bouncermodels} installed silently got the
leaked baseline in preference to the corrected one sitting on disk
(bouncerverse#50).
}
\keyword{internal}
