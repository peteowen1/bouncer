% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cricinfo_test_win_probability.R
\name{build_cricinfo_test_win_probability}
\alias{build_cricinfo_test_win_probability}
\title{Build Test Win Probability for Every Cricinfo Delivery}
\usage{
build_cricinfo_test_win_probability(
  conn = NULL,
  models_path = NULL,
  write = TRUE,
  table_name = "bouncer_wp_from_cricinfo",
  detail_table_name = "bouncer_wp_from_cricinfo_test_detail"
)
}
\arguments{
\item{conn}{DBI connection. If NULL, opens one (write access when
\code{write = TRUE}) and closes it on exit.}

\item{models_path}{Character. Directory holding the in-match models; NULL
resolves via \code{\link[=load_in_match_models]{load_in_match_models()}}.}

\item{write}{Logical. Write the tables, or just return the scored frame.}

\item{table_name}{Character. Scalar target table in the \code{main} schema.}

\item{detail_table_name}{Character. Three-way detail table.}
}
\value{
data.table with one row per scored delivery, including
\code{p_team1_win}, \code{p_draw}, \code{p_team2_win}, \code{p_result}, the before/after/
delta triple on P(team1 wins), and the projected-innings-total triple.
Invisibly when \code{write = TRUE}.
}
\description{
Scores every Test delivery in \code{cricinfo.balls} with the decomposed v3
models and writes P(team batting first wins) to
\code{main.bouncer_wp_from_cricinfo} (format \code{TEST}), alongside a detail
table carrying the full three-way decomposition (win / draw / loss and
P(result)) that the scalar column cannot hold.
}
