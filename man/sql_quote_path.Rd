% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_helpers.R
\name{sql_quote_path}
\alias{sql_quote_path}
\title{Quote a File Path for Interpolation into DuckDB SQL}
\usage{
sql_quote_path(path)
}
\arguments{
\item{path}{Character vector of paths. Backslashes are converted to forward
slashes, which DuckDB accepts on Windows.}
}
\value{
Character vector, escaped and ready to sit between single quotes.
The quotes themselves are NOT added — the caller's format string has them.
}
\description{
Escapes a filesystem path for use inside a single-quoted SQL string, such
as \code{read_parquet('...')}. DuckDB's file-reading functions take the path as
a string literal and cannot be parameterised with a \verb{?} placeholder, so
interpolation is unavoidable here — but it must still be escaped.
}
\details{
Paths were previously interpolated raw, so any path containing an
apostrophe (\verb{O'Brien/data.parquet}, and Windows user directories in
particular) produced a syntax error or, worse, a query that parsed into
something unintended.
}
\examples{
\dontrun{
sprintf("SELECT * FROM read_parquet('\%s')", sql_quote_path(fp))
}

}
\keyword{internal}
