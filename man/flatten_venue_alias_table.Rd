% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/venue_canonical.R
\name{flatten_venue_alias_table}
\alias{flatten_venue_alias_table}
\title{Flatten Multi-Hop Chains and Cycles in venue_aliases}
\usage{
flatten_venue_alias_table(conn = NULL, dry_run = TRUE)
}
\arguments{
\item{conn}{DBI connection with write access; opened and closed if NULL.}

\item{dry_run}{Logical. TRUE (the default) reports what would change and
writes nothing.}
}
\value{
data.table of the rows that would be, or were, repointed, invisibly.
}
\description{
\code{venue_aliases} must maintain the invariant that no \code{canonical_venue} is
ever also an \code{alias} key -- a single-hop lookup (this table's own normal
read pattern, \code{match(venue, alias)}) silently stops at an intermediate
name otherwise. This bit twice (2026-08-29 commit d6c80c1, and again
2026-09-01 reconciling a large batch of new rows): adding a row whose
canonical target is itself an existing alias key creates a chain or, if
the existing row points back the other way, a real cycle.
}
\details{
Ground truth, not an arbitrary tie-break: every node visited while
resolving a chain is checked against how many rows it actually has in
\code{cricsheet.matches} right now, and the one with the most real rows wins.
A phantom string (0 rows) never wins against a populated one, and a
2-cycle is resolved the same way rather than by insertion order.
}
