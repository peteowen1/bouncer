% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rating_validation.R
\name{build_rating_frame}
\alias{build_rating_frame}
\title{Build the (Origin, Player) Evaluation Frame}
\usage{
build_rating_frame(pool, ratings, rating_cols, origins = RATING_VAL_ORIGINS)
}
\arguments{
\item{pool}{Output of \code{\link[=load_rating_pool]{load_rating_pool()}}.}

\item{ratings}{A table with player_id, match_date and one or more rating columns.
The rating used at an origin is the chronologically last row strictly before
that origin. Sorted internally, so the caller need not pre-sort.}

\item{rating_cols}{Character vector of rating column names to carry through.}

\item{origins}{Evaluation origins. Defaults to \link{RATING_VAL_ORIGINS}.}
}
\value{
A \code{data.table}, one row per (origin, qualifying player).
}
\description{
For each origin, assembles each qualifying player's pre-origin career totals, their
recency-weighted totals across the half-life grid, their realised outcomes in the
following 12 months, and their rating as of the origin.
}
