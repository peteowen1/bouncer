% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_helpers.R
\name{validate_delivery_data}
\alias{validate_delivery_data}
\title{Validate Delivery Data}
\usage{
validate_delivery_data(
  df,
  required_cols = NULL,
  context = "validate_delivery_data"
)
}
\arguments{
\item{df}{Data frame to validate.}

\item{required_cols}{Character vector of required column names.
If NULL (default), uses standard delivery columns.}

\item{context}{Character. Context for error message (e.g., function name).}
}
\value{
Invisibly returns TRUE if validation passes. Stops with error if not.
}
\description{
Checks that a data frame has the required columns for delivery-level analysis.
This is the minimum validation for any delivery data processing.
}
\examples{
\dontrun{
# Validates that df has required columns
validate_delivery_data(df)

# Custom required columns
validate_delivery_data(df, required_cols = c("match_id", "over", "ball"))
}

}
