% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_projection_stage2.R
\name{fit_test_stage2_correction}
\alias{fit_test_stage2_correction}
\title{Fit the stage-2 correction for a match_type}
\usage{
fit_test_stage2_correction(
  conn,
  match_type = c("Test", "MDM"),
  overs_model = NULL,
  eis = NULL,
  as_at = NULL
)
}
\arguments{
\item{conn}{DBI connection.}

\item{match_type}{Character. "Test" or "MDM".}

\item{overs_model}{A fit_test_overs_model() result for the SAME match_type
(or NULL to load it). Stage 2 is trained using stage-1 values computed
from this model's PREDICTED balls-remaining, matching how stage1 will
actually be computed at inference time -- not the true observed value,
which would leak information stage 2 will never have when scoring a
live ball.}

\item{eis}{Numeric. Expected initial score for this match_type
(EIS_TEST_MALE_INTL or EIS_TEST_MALE_CLUB).}

\item{as_at}{Date. NULL (default) uses all data through today.}
}
\value{
A list: fit (the lm), match_type, eis, cut_date, n_rows,
n_excluded_degenerate, fitted_at.
}
\description{
Fit the stage-2 correction for a match_type
}
\keyword{internal}
