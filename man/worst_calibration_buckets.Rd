% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration_audit.R
\name{worst_calibration_buckets}
\alias{worst_calibration_buckets}
\title{Surface the most-biased buckets from a calibration audit}
\usage{
worst_calibration_buckets(audit, n = 10, cut = NULL)
}
\arguments{
\item{audit}{A \code{calibration_audit} data frame from \code{\link[=calibration_audit]{calibration_audit()}}.}

\item{n}{Number of rows to return per cut (default 10). Use \code{Inf} for all.}

\item{cut}{Optional character vector restricting to specific cut names.}
}
\value{
A data frame, the worst \code{n} judged buckets per cut, sorted by
\code{abs_bias} descending.
}
\description{
Sorts judged buckets (\code{flag} \code{"ok"} or \code{"high_bias"}) by \code{abs(bias)}
descending. The leak this tool generalises from was invisible in the
aggregate and obvious at the boundary -- looking at the average calibration
would not have caught it; looking at the worst bucket did.
}
