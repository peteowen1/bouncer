% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration_audit.R
\name{calibration_audit}
\alias{calibration_audit}
\title{Per-bucket calibration audit across arbitrary cuts}
\usage{
calibration_audit(predicted, actual, cuts, min_n = 30, flag_threshold = NULL)
}
\arguments{
\item{predicted}{Numeric vector of model predictions.}

\item{actual}{Numeric vector of realised outcomes, same length as
\code{predicted}.}

\item{cuts}{A named list of cut vectors, each the same length as
\code{predicted}/\code{actual}. Names become the \code{cut} column. Rows with \code{NA} in
the cut, prediction or actual are excluded from that cut's buckets.}

\item{min_n}{Minimum bucket size to be judged (default 30). Buckets below
this are flagged \code{"not_judged"}, not dropped.}

\item{flag_threshold}{Optional. If supplied, judged buckets with
\code{abs(bias) > flag_threshold} are flagged \code{"high_bias"} instead of
\code{"ok"}. Leave \code{NULL} (default) to rely on \code{\link[=worst_calibration_buckets]{worst_calibration_buckets()}}
for surfacing the worst offenders instead of a hard cutoff.}
}
\value{
A data frame (class \code{calibration_audit}) with one row per bucket
per cut: \code{cut}, \code{bucket}, \code{n}, \code{judged}, \code{mean_predicted}, \code{mean_actual},
\code{bias} (predicted minus actual), \code{abs_bias}, \code{flag}
(\code{"ok"}/\code{"high_bias"}/\code{"not_judged"}/\code{"single_bucket"}/\code{"no_data"}).
}
\description{
For each cut variable supplied in \code{cuts}, buckets \code{predicted}/\code{actual} by
the distinct values of that variable and reports the mean prediction, mean
actual outcome, and bias (predicted minus actual) per bucket. This is the
generalised form of the check that caught the agnostic-model post-delivery
leak (bouncerverse, D-P38): aggregate calibration was healthy throughout,
and the leak was only visible in specific buckets (the first ball of an
innings).
}
\details{
Every cut is optional -- pass whichever cut variables you have (ball
number, over, innings number, wickets fallen, phase, margin bucket,
competition, venue country, home/away, gender, format, season, ...). Each
element of \code{cuts} must be the same length as \code{predicted} and \code{actual}.

A bucket with fewer than \code{min_n} rows is reported but flagged
\code{"not_judged"} rather than dropped -- a biased bucket of 12 balls is noise,
but a cut that examined nothing must not read as a pass either. A cut
whose values are constant (a single bucket) cannot compare anything and is
flagged \code{"single_bucket"} for every row of that cut, never \code{"ok"}.
}
\seealso{
\code{\link[=worst_calibration_buckets]{worst_calibration_buckets()}}, \code{\link[=audit_low_information_state]{audit_low_information_state()}}
}
