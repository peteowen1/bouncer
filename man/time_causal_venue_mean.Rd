% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/venue_rates.R
\name{time_causal_venue_mean}
\alias{time_causal_venue_mean}
\title{Mean of a per-match value at a venue, using only earlier matches}
\usage{
time_causal_venue_mean(
  matches,
  value_col,
  prior_weight = 5,
  prior_value = NULL
)
}
\arguments{
\item{matches}{data.table with \code{match_id}, \code{venue}, \code{match_date} and the
column named by \code{value_col}. Rows where that value is \code{NA} contribute
nothing but still receive an estimate.}

\item{value_col}{Name of the numeric column to average.}

\item{prior_weight}{Strength of the prior, in matches. Default 5 — lower
than the result-rate default because a total is far less noisy than a
single binary outcome.}

\item{prior_value}{Value to shrink toward. NULL means the global mean.}
}
\value{
data.table with \code{match_id}, \code{venue_mean}, \code{n_prior}, \code{at_prior}.
Carries \code{prior_value} as an attribute.
}
\description{
The numeric sibling of \code{\link[=time_causal_venue_result_rate]{time_causal_venue_result_rate()}}, for features like
"average first-innings total at this ground". Same defect, same shape: a
venue average computed over every match at the ground \strong{includes the match
being predicted}, and at a one-match venue the feature simply \emph{is} that
match's own total.
}
\keyword{internal}
