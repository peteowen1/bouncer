% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_connection.R
\name{connect_to_bouncer}
\alias{connect_to_bouncer}
\title{Connect to Bouncer Database}
\usage{
connect_to_bouncer(path = NULL, read_only = FALSE)
}
\arguments{
\item{path}{Character string specifying the database file path. If NULL,
uses the default system data directory.}

\item{read_only}{Logical. If TRUE, opens database in read-only mode.
Default is FALSE.}
}
\value{
A DuckDB connection object. Remember to disconnect when done using
\code{disconnect_bouncer(conn)}.
}
\description{
User-facing function to connect to the Bouncer cricket database.
Use this connection to query data directly with SQL if needed.
}
\examples{
\dontrun{
# Connect to database
conn <- connect_to_bouncer()

# Query data
matches <- DBI::dbGetQuery(conn, "SELECT * FROM matches LIMIT 10")

# Always disconnect when done
disconnect_bouncer(conn)
}
}
