% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_player.R
\name{find_player}
\alias{find_player}
\title{Find a Player by Name}
\usage{
find_player(query, format = NULL, gender = NULL, conn = NULL, quiet = FALSE)
}
\arguments{
\item{query}{Character. A name, surname, or fragment.}

\item{format, gender}{Optional bucket for the career-ball counts used to rank
candidates. NULL counts across everything.}

\item{conn}{DBI connection; opened read-only and closed on exit if NULL.}

\item{quiet}{Logical. Suppress the ambiguity warning (it still returns all
matches).}
}
\value{
data.table of \code{player_id}, \code{player_name}, \code{balls}, \code{matches},
\code{match_type} (which tier matched), best-evidenced first. Zero rows if
nothing matched — check \code{nrow()}, never assume.
}
\description{
Resolves a name or fragment to registry entries, loudest-first on ambiguity.
}
\details{
Matching is tried in order and stops at the first tier that hits: exact
name, then case-insensitive exact, then surname, then substring. That way
\code{"V Kohli"} does not silently fall through to a substring match on some
other Kohli, while \code{"Ecclestone"} still finds \code{"S Ecclestone"}.

Results carry \code{balls} so the well-established player sorts first, and a
warning fires whenever more than one player matches — the case that has
produced wrong answers before.
}
