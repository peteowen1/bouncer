% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/in_match_prediction.R
\name{fill_model_features}
\alias{fill_model_features}
\title{Fill Missing Model Features, Loudly}
\usage{
fill_model_features(data, feature_cols, what = "model", max_missing_frac = 0.1)
}
\arguments{
\item{data}{data.frame of features supplied by the caller.}

\item{feature_cols}{Character vector the model was trained on.}

\item{what}{Character. Model name, for the message.}

\item{max_missing_frac}{Numeric. Abort above this proportion missing.
Default 0.1 -- a tenth of the feature space silently zeroed is already
enough to make the output meaningless.}
}
\value{
\code{data} with any missing columns added as 0.
}
\description{
Every in-match predictor used to do \code{for (col in feature_cols) if (!col \%in\% names(data)) data[[col]] <- 0} -- silently substituting zero for any feature
the caller failed to supply.
}
\details{
That is how bouncer's ODI chase model came to be useless in production
without anyone noticing. \code{predict_win_probability()} builds 12 features; the
stage-2 model was trained on 44. The other 32 -- \code{runs_needed}-derived chase
features, every momentum window, \code{innings1_total} -- were zero-filled on
every call, so the model saw a nonsense vector and returned a near-constant
answer: 0.883 when the chase needed 5 runs off 60 balls, 0.947 when it needed
200 off 30. Benchmarked against 20,326 real ODI deliveries it scored a Brier
of 0.312, worse than predicting 0.5 every ball (0.250), while the scraped
ESPNcricinfo number scored 0.221.

Zero is not a neutral value for a tree model. It is a real point in feature
space, usually far outside the training distribution, and the model answers
confidently from it. Missing features must therefore be loud.
}
\keyword{internal}
