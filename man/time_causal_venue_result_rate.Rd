% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/venue_rates.R
\name{time_causal_venue_result_rate}
\alias{time_causal_venue_result_rate}
\title{Historical result rate at a venue, using only earlier matches}
\usage{
time_causal_venue_result_rate(matches, prior_weight = 10, prior_rate = NULL)
}
\arguments{
\item{matches}{data.table (or data.frame) with one row per match and columns
\code{match_id}, \code{venue}, \code{match_date}, \code{decided} (1 if the match reached a
decided outcome and so counts toward the denominator) and \code{is_result}
(1 if that outcome was a win rather than a draw).}

\item{prior_weight}{Strength of the Bayesian prior, in matches. Default 10.}

\item{prior_rate}{Rate to shrink toward. Default NULL, meaning the global
decided-match result rate over the whole table.}
}
\value{
data.table with \code{match_id}, \code{venue_result_rate}, \code{n_prior} (decided
matches at that ground before this one) and \code{at_prior} (TRUE where there is
no earlier history, so the value IS the prior). Carries \code{prior_rate} as an
attribute.
}
\description{
Historical result rate at a venue, using only earlier matches
}
\section{Ties on date}{

Two matches at the same ground on the same date cannot see each other
either — a prediction made for one has no access to the other's result. Rows
sharing a (venue, date) therefore all see the same strictly-earlier history.
}

\section{The prior}{

\code{prior_rate} is global and does include the current match, which is a leak of
weight \code{1/n_total} — about 0.05\% against the 7.7-9.1\% the per-venue term
carried. Left global deliberately: making it expanding too buys ~0.05\% and
costs stability on the first few matches ever played.
}

\keyword{internal}
