% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/team_elo.R
\name{calculate_mov_multiplier}
\alias{calculate_mov_multiplier}
\title{Calculate Margin of Victory Multiplier}
\usage{
calculate_mov_multiplier(
  unified_margin,
  winner_elo,
  loser_elo,
  format = "t20",
  mov_exponent = MOV_EXPONENT,
  mov_base_offset = MOV_BASE_OFFSET,
  mov_denom_base = MOV_DENOMINATOR_BASE,
  mov_elo_factor = MOV_ELO_FACTOR,
  mov_min = MOV_MIN,
  mov_max = MOV_MAX
)
}
\arguments{
\item{unified_margin}{Numeric. Margin of victory in runs-equivalent.
Positive = winner's perspective, use abs() for magnitude.}

\item{winner_elo}{Numeric. ELO rating of the winning team}

\item{loser_elo}{Numeric. ELO rating of the losing team}

\item{format}{Character. Match format for potential format-specific tuning}

\item{mov_exponent}{Numeric. Exponent for margin (default from constants)}

\item{mov_base_offset}{Numeric. Base offset to prevent G=0 (default from constants)}

\item{mov_denom_base}{Numeric. Denominator base (default from constants)}

\item{mov_elo_factor}{Numeric. ELO factor for favorite adjustment (default from constants)}

\item{mov_min}{Numeric. Minimum G value (default from constants)}

\item{mov_max}{Numeric. Maximum G value (default from constants)}
}
\value{
Numeric. Multiplier (typically 0.5 to 2.5)
}
\description{
Calculates the G-factor (margin of victory multiplier) for ELO updates.
Based on FiveThirtyEight methodology used in NFL/NBA.
}
\details{
The multiplier rewards dominant victories and punishes narrow losses,
while adjusting for favorite bias (favorites are expected to win by more).

Formula: G = (abs(margin) + BASE_OFFSET)^EXPONENT / (DENOM_BASE + ELO_FACTOR * elo_diff)
Bounded by MOV_MIN and MOV_MAX.
}
\keyword{internal}
