% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/three_way_elo_tables.R
\name{promote_3way_elo_staging}
\alias{promote_3way_elo_staging}
\title{Promote a Completed 3-Way ELO Staging Table Over the Live One}
\usage{
promote_3way_elo_staging(
  category,
  conn,
  min_rows = 1L,
  min_fraction_of_live = 0.9
)
}
\arguments{
\item{category}{Character, e.g. \code{"mens_t20"}.}

\item{conn}{A DBI connection with write access.}

\item{min_rows}{Integer. Refuse to promote fewer rows than this. A staging
table that is empty or tiny means the rebuild failed, and promoting it
would destroy the ratings it was meant to replace.}

\item{min_fraction_of_live}{Numeric. Also refuse to promote a table holding
less than this share of the CURRENT live table. \code{min_rows} only compares
against what the run itself expected, so a deliberately limited run --
\code{MATCH_LIMIT} set for a smoke test -- would pass it and then replace a
full table with a handful of matches. Set to \code{NULL} to allow a genuine
shrink (a format being rebuilt from a smaller corpus).}
}
\value{
Invisibly, the number of rows promoted.
}
\description{
Replaces the live table with the staging table in a single transaction, so
there is no point at which a reader sees an empty table.
}
\keyword{internal}
