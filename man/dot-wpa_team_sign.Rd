% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leverage.R
\name{.wpa_team_sign}
\alias{.wpa_team_sign}
\title{Batting-team sign for a per-ball win-probability delta}
\usage{
.wpa_team_sign(striker_team_id, team1_id, innings_number)
}
\arguments{
\item{striker_team_id, team1_id}{Integer/character vectors. \code{team1_id} is
the team that batted in innings 1; NA in either falls back to
\code{innings_number}'s parity.}

\item{innings_number}{Integer vector, used only where a team id is missing.}
}
\value{
Numeric vector of 1/-1, same length as the inputs.
}
\description{
+1 when the striker's team is the side that batted first, -1 otherwise --
the same construction as \code{.wp_source_sql()}'s \code{team_sign} CASE expression
in \code{player_game_data.R}. Kept as a separate R implementation (that one
lives in SQL) rather than a shared call, so if the two ever need to be
reconciled, this docstring is the pointer.
}
\keyword{internal}
