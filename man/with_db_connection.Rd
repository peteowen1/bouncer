% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_connection.R
\name{with_db_connection}
\alias{with_db_connection}
\title{Run an Expression Against a Scoped Database Connection}
\usage{
with_db_connection(fn, path = NULL, read_only = FALSE)
}
\arguments{
\item{fn}{Function of one argument, the connection.}

\item{path}{Character. Database path, passed to \code{\link[=get_db_connection]{get_db_connection()}}.}

\item{read_only}{Logical. Open read-only. Default FALSE.}
}
\value{
Whatever \code{fn} returns.
}
\description{
Opens a connection, passes it to \code{fn}, and disconnects on the way out
whether \code{fn} returned or threw.
}
\section{Why this exists}{

DuckDB permits exactly one write connection at a time, so a write
connection leaked by an error holds the lock for the remainder of the R
session — every later write fails with "Could not set lock", far from the
code that caused it. The \verb{open / do work / dbDisconnect} sequence written
without \code{on.exit} is safe only on the happy path; this makes the disconnect
unconditional. Prefer it to hand-rolling \code{on.exit} at each call site.

Not a fit when the connection outlives the call — \code{\link[=connect_to_bouncer]{connect_to_bouncer()}}
hands ownership to its caller and must not use this.
}

\examples{
\dontrun{
n <- with_db_connection(function(conn) {
  DBI::dbGetQuery(conn, "SELECT COUNT(*) AS n FROM cricsheet.matches")$n
}, read_only = TRUE)
}

}
\keyword{internal}
