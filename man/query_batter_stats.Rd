% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_queries.R
\name{query_batter_stats}
\alias{query_batter_stats}
\title{Query Batter Stats}
\usage{
query_batter_stats(
  batter_id = NULL,
  bowler_id = NULL,
  match_type = NULL,
  season = NULL,
  venue = NULL,
  city = NULL,
  country = NULL,
  event = NULL,
  date_range = NULL,
  batting_team = NULL,
  bowling_team = NULL,
  min_balls = 0,
  db_path = NULL
)
}
\arguments{
\item{batter_id}{Character. The batter to get stats for. If NULL, returns
stats for all batters matching other filters.}

\item{bowler_id}{Character. Filter to only deliveries from this bowler (for head-to-head)}

\item{match_type}{Character. Filter by match type (e.g., "t20", "odi", "test")}

\item{season}{Character or numeric. Filter by season}

\item{venue}{Character. Filter by venue/stadium (partial match)}

\item{city}{Character. Filter by city (partial match)}

\item{country}{Character. Filter by country (uses venue-to-country mapping)}

\item{event}{Character. Filter by event name (partial match)}

\item{date_range}{Date vector of length 2. c(start_date, end_date)}

\item{batting_team}{Character. Filter by batting team}

\item{bowling_team}{Character. Filter by bowling team}

\item{min_balls}{Integer. Minimum balls faced to include (default 0)}

\item{db_path}{Character. Database path}
}
\value{
Data frame with batting statistics:
\itemize{
\item batter_id - Player identifier
\item balls_faced - Total balls faced
\item runs_scored - Total runs scored
\item dismissals - Times dismissed
\item dots - Dot balls (0 runs)
\item fours - Number of fours
\item sixes - Number of sixes
\item batting_average - Runs per dismissal
\item runs_per_ball - Runs per ball faced
\item strike_rate - Runs per 100 balls
\item wicket_pct - Dismissal percentage per ball
}
}
\description{
Get aggregated batting statistics with flexible filters. If batter_id is
provided, returns stats for that player. If omitted, returns stats for all
batters matching the filters (grouped by batter).
}
\examples{
\dontrun{
# Single player stats
query_batter_stats("V Kohli", match_type = "t20")

# Head-to-head vs specific bowler
query_batter_stats("V Kohli", bowler_id = "J Bumrah")

# All batters in Test cricket (min 100 balls)
query_batter_stats(match_type = "test", min_balls = 100)

# All batters in IPL
query_batter_stats(event = "Indian Premier League", min_balls = 50)

# All batters in Australia
query_batter_stats(country = "Australia", match_type = "test", min_balls = 100)
}
}
