% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/player_id_canonical.R
\name{build_player_id_map}
\alias{build_player_id_map}
\title{Build a Canonical Player ID Map}
\usage{
build_player_id_map(conn = NULL)
}
\arguments{
\item{conn}{DBI connection; opened read-only and closed on exit if NULL.}
}
\value{
data.table of \code{player_id} (the bare-name id to retire),
\code{canonical_id}, \code{player_name} and \code{apps}.
}
\description{
Folds bare-name player ids onto the registry hash id for the same person.
}
\details{
Three exclusions, all deliberately conservative:
\itemize{
\item the two ids appear in the SAME match — proof of two different
people, since one person cannot bat twice in an innings as two ids. One
name is excluded this way (\verb{E Jones}).
\item two or more hash ids share the display name, so there is no way to
pick a target (57 names, e.g. three different \verb{A Sharma}s).
\item no hash id shares the name at all — a genuinely unregistered
player, with nothing to merge into (872 ids).
}

930 bare-name ids are left alone in total, carrying 0.45\% of appearances:
those 872 plus the 58 caught by the two exclusions above.
}
