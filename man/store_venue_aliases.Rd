% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/venue_canonical.R
\name{store_venue_aliases}
\alias{store_venue_aliases}
\title{Store Derived Aliases in the Existing venue_aliases Table}
\usage{
store_venue_aliases(map, conn = NULL, dry_run = TRUE)
}
\arguments{
\item{map}{Output of \code{\link[=build_venue_id_map]{build_venue_id_map()}}.}

\item{conn}{DBI connection with write access; opened and closed if NULL.}

\item{dry_run}{Logical. TRUE (the default) reports what would change and
writes nothing. Populating this table moves every venue-keyed feature, so
the write is opt-in.}
}
\value{
data.table of the rows that would be, or were, inserted.
}
\description{
There is already a \code{venue_aliases} table and a resolver
(\code{\link[=normalize_venue]{normalize_venue()}}, \code{\link[=add_venue_alias]{add_venue_alias()}}), so derived aliases belong THERE
rather than in a second mechanism. \code{\link[=build_venue_id_map]{build_venue_id_map()}} supplies content;
this writes it.
}
\section{What the existing table contains}{

98 hand-curated rows, and only \strong{27 of their aliases appear as a cricsheet
venue name at all} -- they are abbreviations like \code{MCG}, \code{SCG}, \code{Gabba} and
\code{WACA} written against a different naming convention. So the machinery is
real and its content largely does not match the corpus it is applied to. The
derivation adds 116 aliases that DO occur in cricsheet.
}

