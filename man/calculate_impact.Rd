% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/player_career_ratings.R
\name{calculate_impact}
\alias{calculate_impact}
\title{Calculate Player Impact Ratings}
\usage{
calculate_impact(
  format = c("t20", "odi", "test"),
  player_game_data = NULL,
  ref_date = NULL,
  decay_batting = NULL,
  decay_bowling = NULL,
  prior_matches = IMPACT_PRIOR_MATCHES,
  prior_rate = IMPACT_PRIOR_RATE
)
}
\arguments{
\item{format}{Character. "t20", "odi", or "test". Test aborts until its
inputs exist (\code{\link[=get_impact_kappa]{get_impact_kappa()}}).}

\item{player_game_data}{data.table from \code{\link{load_player_game_data}}.
If NULL, loads automatically.}

\item{ref_date}{Date. Compute the rating as of this date (NULL = latest + 1).}

\item{decay_batting}{Numeric. Decay constant in days for batting component.}

\item{decay_bowling}{Numeric. Decay constant in days for bowling component.}

\item{prior_matches}{Numeric. Prior pseudo-matches for shrinkage.}

\item{prior_rate}{Numeric. Prior rate (typically 0 = replacement level).}
}
\value{
data.table with one row per player:
\describe{
\item{player_id}{Player identifier}
\item{role_group}{BATTER, BOWLER, ALL_ROUNDER}
\item{batting_impact}{Career batting value rating (run units)}
\item{bowling_impact}{Career bowling value rating (run units)}
\item{total_impact}{batting_impact + bowling_impact}
\item{n_matches, wt_matches}{Match count and weighted count}
}
}
\description{
Computes rolling career value ratings for each player from per-match RAA
and WPA: \code{value = raa + kappa * wpa}, where kappa is the fitted
runs-per-win-probability exchange rate (\code{\link[=get_impact_kappa]{get_impact_kappa()}}). Uses
exponential time decay, Bayesian shrinkage toward replacement level, and
exposure weighting -- per component, so batting and bowling decay
independently.
}
\details{
Both inputs are bouncer's own: RAA from \code{\link[=build_cricinfo_raa]{build_cricinfo_raa()}} (agnostic
baseline, fitted wicket value) and WPA from
\code{\link[=build_cricinfo_win_probability]{build_cricinfo_win_probability()}} via \code{player_game_data.R}, own-team
signed (bouncerverse#25). A match with either component missing stays NA
and drops out of numerator AND denominator together -- the coverage warning
below is load-bearing, do not silence it.
}
