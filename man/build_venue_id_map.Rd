% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/venue_canonical.R
\name{build_venue_id_map}
\alias{build_venue_id_map}
\title{Build a Venue Name Crosswalk}
\usage{
build_venue_id_map(conn = NULL, min_matches = 1L)
}
\arguments{
\item{conn}{DBI connection; opened read-only and closed on exit if NULL.}

\item{min_matches}{Integer. Ignore names with fewer than this many matches
when deciding a canonical target. Default 1.}
}
\value{
data.table with \code{venue} (the alias) and \code{canonical_venue}. Carries
two attributes for inspection: \code{ambiguous}, the names spanning several
cities, and \code{review}, coordinate groups that were NOT merged and why.
}
\description{
Maps alias venue names to a canonical one. Mirrors
\code{\link[=build_player_id_map]{build_player_id_map()}}: conservative, auditable, and it declines rather
than guesses.
}
