% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/venue_rates.R
\name{time_causal_hierarchical_mean_decayed}
\alias{time_causal_hierarchical_mean_decayed}
\title{Mean of a per-match value, shrunk through a chain of nested causal levels,
with exponential recency decay}
\usage{
time_causal_hierarchical_mean_decayed(
  matches,
  value_col,
  levels,
  weights,
  half_life,
  root_prior_weight = 30,
  root_prior_value = NULL
)
}
\arguments{
\item{matches}{data.table with \code{match_id}, \code{match_date}, every column named
in \code{levels}, and the column named by \code{value_col}.}

\item{value_col}{Name of the numeric column to average.}

\item{levels}{Character vector of grouping columns, ordered FINEST to
COARSEST (e.g. \code{c("venue", "competition")}). The row's estimate is the
finest level's causal mean, shrunk toward the next level up.}

\item{weights}{Named numeric vector of prior weights (in matches), one per
entry of \code{levels}, keyed by the same names.}

\item{half_life}{Named numeric vector, in days, one per entry of \code{levels}
(same names as \code{weights}) PLUS an entry named \code{"root"} for the root
level. \code{Inf} for a level means "no decay" (behaves exactly like
\code{\link[=time_causal_hierarchical_mean]{time_causal_hierarchical_mean()}} at that level) -- so a hierarchy can
decay some levels and not others.}

\item{root_prior_weight}{Prior weight (in matches) regularizing the root
causal running mean toward \code{root_prior_value}. Default 30.}

\item{root_prior_value}{Value the root level shrinks toward. NULL (default)
means the whole-sample mean of \code{value_col} over \code{matches} -- matching
\code{\link[=time_causal_venue_mean]{time_causal_venue_mean()}}'s \code{prior_value = NULL} behaviour, including
that this constant is shared by every row and is NOT itself computed
causally match-by-match (it is one fixed scalar, the same as every other
caller of this codebase's shrinkage-to-a-global-average pattern).}
}
\value{
Same shape as \code{\link[=time_causal_hierarchical_mean]{time_causal_hierarchical_mean()}}.
}
\description{
The decayed sibling of \code{\link[=time_causal_hierarchical_mean]{time_causal_hierarchical_mean()}}. That function's
causal running mean at every level weighs a match from 15 years ago
identically to one from last week -- correct for a level with a genuinely
stable rate, wrong for one that is actually drifting (bouncerverse#84/#85:
IPL's league-wide scoring rate rose from 1.34 to 1.56 runs/ball between
2022 and 2026 while the flat causal mean stayed pinned near 1.25, a
calibration gap 40x the whole-corpus average). \code{half_life} lets each level
forget slowly-obsoleting history rather than average across all of it
uniformly -- matching hganjoo's T20 Metrics primer's own league/year/ground
nested-shrinkage design (a year LEVEL in a hierarchy) with a continuous
decay instead of discrete year buckets, which avoids a hard reset to the
coarse prior at the boundary of every new season.
}
\keyword{internal}
