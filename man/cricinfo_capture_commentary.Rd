% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cricinfo_scraper.R
\name{cricinfo_capture_commentary}
\alias{cricinfo_capture_commentary}
\title{Capture Ball-by-Ball Commentary via Page Scroll}
\usage{
cricinfo_capture_commentary(
  browser,
  series_id,
  match_id,
  scroll_pause = 1.5,
  max_scrolls = 150,
  stale_threshold = 10,
  all_innings = TRUE
)
}
\arguments{
\item{browser}{A ChromoteSession object with Network enabled}

\item{series_id}{Integer series objectId}

\item{match_id}{Integer match objectId}

\item{scroll_pause}{Seconds between scroll actions (default 1.5)}

\item{max_scrolls}{Maximum number of scroll actions (default 150)}

\item{stale_threshold}{Stop after this many scrolls with no new data (default 10)}

\item{all_innings}{Logical. Whether to capture all innings (default TRUE)}
}
\value{
List with $initial_data (from \strong{NEXT_DATA}) and $api_responses (from scroll)
}
\description{
Navigates to the ball-by-ball commentary page, then scrolls gradually
to trigger the page's own API calls for loading earlier overs. Captures
all API responses via CDP Network interception.
}
\details{
Returns commentary for the latest innings. The page always loads the
most recent innings first; innings switching is not currently supported
via the page UI.
}
\keyword{internal}
