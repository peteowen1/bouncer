% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/in_match_prediction.R
\name{.load_inmatch_results}
\alias{.load_inmatch_results}
\title{Load an in-match model results bundle, bouncermodels first}
\usage{
.load_inmatch_results(model_name, local_path, allow_release = TRUE)
}
\arguments{
\item{model_name}{Character. bouncermodels registry name, e.g.
"t20_stage1_results".}

\item{local_path}{Character. Full path to the local \code{.rds} fallback.}

\item{allow_release}{Logical. FALSE skips bouncermodels entirely and reads
only \code{local_path} -- set by callers when the caller supplied an explicit
\code{models_path}, the same override contract \code{load_agnostic_model()} and
\code{load_margin_model()} honor via \code{is.null(model_dir)}. Without this, a
caller pointing at a specific on-disk artefact (e.g. testing a fresh
retrain before publishing) would silently get the release instead.}
}
\value{
The deserialized list (\verb{$model}, \verb{$feature_cols}, ...), or NULL if
neither source has it.
}
\description{
\verb{\{model_name\}.rds} bundles \verb{$model} (an xgb.Booster) with \verb{$feature_cols}
and training metadata in one object -- both stage1 and stage2, and the
decomposed Test v3 pair, are shaped this way. Mirrors the
bouncermodels-then-local-fallback pattern in \code{load_agnostic_model()}
(\code{R/agnostic_model.R}), which the in-match/prediction tags never used
before this (bouncerverse#81 / D-P54) despite being published there.
}
\keyword{internal}
