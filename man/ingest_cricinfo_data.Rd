% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cricinfo_data.R
\name{ingest_cricinfo_data}
\alias{ingest_cricinfo_data}
\title{Ingest Cricinfo Data into DuckDB}
\usage{
ingest_cricinfo_data(
  cricinfo_dir = NULL,
  path = NULL,
  formats = c("t20i", "odi", "test"),
  genders = c("male", "female"),
  verbose = TRUE
)
}
\arguments{
\item{cricinfo_dir}{Character. Path to the cricinfo data directory
(e.g., "../bouncerdata/cricinfo"). If NULL, auto-detects from
bouncerdata sibling directory.}

\item{path}{Character. Database file path. If NULL, uses default.}

\item{formats}{Character vector. Formats to ingest. Default
c("t20i", "odi", "test").}

\item{genders}{Character vector. Genders to ingest. Default
c("male", "female").}

\item{verbose}{Logical. Print progress messages. Default TRUE.}
}
\value{
Invisibly returns a list with counts of ingested records.
}
\description{
Scans Cricinfo parquet directories for ball-by-ball, match, and innings
data and loads them into DuckDB. Skips matches already in the database.
Also loads the fixtures index if present.
}
\examples{
\dontrun{
# Ingest all formats from default location
ingest_cricinfo_data()

# Ingest only T20I male data
ingest_cricinfo_data(formats = "t20i", genders = "male")

# Ingest from a specific directory
ingest_cricinfo_data(cricinfo_dir = "path/to/cricinfo")
}
}
