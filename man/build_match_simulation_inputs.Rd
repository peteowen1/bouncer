% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_inputs.R
\name{build_match_simulation_inputs}
\alias{build_match_simulation_inputs}
\title{Assemble Match Simulation Inputs}
\usage{
build_match_simulation_inputs(
  conn,
  match_id = NULL,
  team1 = NULL,
  team2 = NULL,
  venue = NULL,
  format = NULL,
  gender = "male",
  team_type = "international",
  team1_players = NULL,
  team2_players = NULL,
  id_map = NULL
)
}
\arguments{
\item{conn}{DBI connection.}

\item{match_id}{Character. If supplied, the historical path is used and
\code{team1}/\code{team2}/\code{venue}/\code{format}/\code{gender}/\code{team_type} come from
\code{cricsheet.matches} instead of the arguments below.}

\item{team1, team2}{Character. Team names as they appear in
\code{cricsheet.matches} (e.g. "India"), not a composite team_id. Required
for a hypothetical fixture; ignored (a message is printed if they
disagree with the match record) when \code{match_id} resolves.}

\item{venue}{Character. Venue name. Same rule as \code{team1}/\code{team2}.}

\item{format}{Character. "t20", "odi", or "test". Same rule.}

\item{gender}{Character. "male" or "female". Default "male". Same rule.}

\item{team_type}{Character. "international" or "club" -- feeds
\code{\link[=make_team_id]{make_team_id()}} for the team-skill lookup. Default "international".
Same rule.}

\item{team1_players, team2_players}{Character vectors of player names.
Required when \code{match_id} is NULL or has no recorded squad. Ignored when
a squad is found.}

\item{id_map}{Optional canonical-id map from \code{\link[=build_player_id_map]{build_player_id_map()}},
forwarded to \code{\link[=build_team_roster_skills]{build_team_roster_skills()}} to fix the ~4-5\% of
\code{main.match_squads} rows that are name-keyed rather than
registry-id-keyed (bouncerverse#74). Building this map is one expensive
full-table scan; build it once and pass it in when assembling many
fixtures (a season/tournament), and omit it for a single match.}
}
\value{
List with \code{match_id}, \code{format}, \code{gender}, \code{team_type}, \code{team1},
\code{team2} (each from \code{\link[=build_team_simulation_inputs]{build_team_simulation_inputs()}}: \code{team}, \code{team_id},
\code{source} ("squad" or "caller_supplied"), \code{batters}, \code{bowlers},
\code{n_players}, \code{n_batters_resolved}, \code{n_bowlers_resolved},
\code{unresolved_players}, \code{team_skills} (\verb{$batting}/\verb{$bowling}, each
\code{runs_skill}/\code{wicket_skill}/\code{resolved})), and \code{venue_skills}.
}
\description{
The missing glue between "team A vs team B at venue V" and the argument
shapes \code{\link[=simulate_match_ballbyball]{simulate_match_ballbyball()}} takes: a batter skill list and a
bowler skill list per team, a team-skill object per team, and a venue
skill object. See the file header for the design rationale.
}
\details{
Two ways to call this:
\itemize{
\item \strong{Historical match}: pass \code{match_id}. \code{team1}/\code{team2}/\code{venue}/\code{format}/
\code{gender}/\code{team_type} are read from \code{cricsheet.matches} and the real XI
from \code{main.match_squads}; any of those arguments passed explicitly are
ignored in favour of the match record (a historical match has ground
truth; use it).
\item \strong{Hypothetical fixture}: leave \code{match_id} NULL and supply \code{team1},
\code{team2}, \code{venue}, \code{format}, \code{team1_players}, \code{team2_players} yourself.
There is no squad to read, so player names are required.
}
}
\section{ELO features are deliberately NOT wired in}{

\code{simulate_delivery()} currently omits \code{elo_run_diff}/\code{elo_wicket_diff}/
\code{elo_venue_run}, and \code{prepare_full_features()} defaults them to 0 when
absent. Whether this assembler should start supplying real ELO diffs is
an open call on bouncerverse#66 (the full model was retrained on real ELO
coverage per bouncerverse#65, but is not yet wired into ANY consumer --
see bouncer/CLAUDE.md's ELO section) -- not decided here, so this
function does not touch it either way.
}

\seealso{
\code{\link[=simulate_match_ballbyball]{simulate_match_ballbyball()}} for what consumes this output.
}
