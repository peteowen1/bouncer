% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rating_validation.R
\name{score_rating}
\alias{score_rating}
\title{Score a Rating Against Both Tuned Baselines}
\usage{
score_rating(
  frame,
  target = c("runs", "events"),
  rating_col,
  origins = RATING_VAL_ORIGINS
)
}
\arguments{
\item{frame}{Output of \code{\link[=build_rating_frame]{build_rating_frame()}}.}

\item{target}{"runs" (runs per ball) or "events" (dismissals or wickets per ball).}

\item{rating_col}{Name of the rating column to score.}

\item{origins}{Origins to iterate. The first is calibration-only and is never scored.}
}
\value{
A \code{data.table}, one row per scored origin, with the losses for the rating and
both baselines, the selected hyperparameters, and Spearman correlations.
}
\description{
Rolling-origin evaluation. At each scored origin, the baseline hyperparameters and the
rating-to-rate calibration are fitted on origins strictly earlier, so nothing leaks.
}
