% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/player_game_data.R
\name{.raa_sql}
\alias{.raa_sql}
\title{SQL Fragments Selecting Per-Ball RAA}
\usage{
.raa_sql(conn)
}
\arguments{
\item{conn}{DBI connection, used to check the table exists.}
}
\value{
List with \code{col} (per-ball RAA expression) and \code{join} (FROM-clause
join, possibly empty).
}
\description{
Runs Above Average is produced per delivery by \code{\link[=build_cricinfo_raa]{build_cricinfo_raa()}} into
\code{main.cricinfo_ball_raa}. Formats whose lambda is not fitted yet (and any
database where the builder has not run) have no rows there -- and possibly
no table -- so the fragment degrades to a NULL column rather than a broken
join, and \code{batting_raa} arrives as NA exactly like an unscored WPA.
}
\keyword{internal}
