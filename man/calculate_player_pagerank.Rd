% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/centrality_interface.R
\name{calculate_player_pagerank}
\alias{calculate_player_pagerank}
\title{Calculate Player PageRank from Database}
\usage{
calculate_player_pagerank(
  conn,
  format = "t20",
  gender = "male",
  min_deliveries = CENTRALITY_MIN_DELIVERIES,
  damping = 0.85,
  verbose = TRUE
)
}
\arguments{
\item{conn}{DBI connection to the bouncer database.}

\item{format}{Character. Match format: "t20", "odi", "test", or "all".}

\item{gender}{Character. Gender filter: "male", "female", or "all".}

\item{min_deliveries}{Integer. Minimum deliveries for inclusion.}

\item{damping}{Numeric. PageRank damping factor.}

\item{verbose}{Logical. Print progress messages.}
}
\value{
List with:
\itemize{
\item batters: Data frame with batter PageRank results
\item bowlers: Data frame with bowler PageRank results
\item matrices: The underlying matchup matrices
\item algorithm: Algorithm metadata (iterations, convergence)
}
}
\description{
High-level function that loads delivery data from the database and computes
PageRank quality scores for all players.
}
\examples{
\dontrun{
conn <- get_db_connection(read_only = TRUE)
pr <- calculate_player_pagerank(conn, format = "t20")
head(pr$batters[order(-pr$batters$pagerank), ])
DBI::dbDisconnect(conn, shutdown = TRUE)
}
}
