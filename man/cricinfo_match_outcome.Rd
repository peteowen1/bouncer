% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cricinfo_win_probability.R
\name{cricinfo_match_outcome}
\alias{cricinfo_match_outcome}
\title{Parse a Cricinfo Match Result From Its Status Text}
\usage{
cricinfo_match_outcome(status_text)
}
\arguments{
\item{status_text}{Character vector of \code{cricinfo.matches.status_text}.}
}
\value{
A data.frame with one row per input:
\itemize{
\item \code{result} — "batting_first", "chasing", "tied", "drawn",
"no_result", or NA when the text is absent or unrecognised
\item \code{bf_won} — 1 if the side batting first won, 0 if the chasing side
won, NA otherwise. Ties are NA even when a Super Over decided them:
the innings themselves were level.
\item \code{margin}, \code{margin_type} — "runs", "wickets" or "innings_and_runs"
\item \code{is_dls} — TRUE when the result was reached by DLS/D-L
\item \code{super_over} — TRUE when a Super Over or one-over eliminator
decided a tie
}
}
\description{
Turns \code{cricinfo.matches.status_text} into a usable outcome. This is the
correct label for any win-probability evaluation in this package.
}
\section{Do not derive the result from the scores}{

\code{innings2_total <= innings1_total} looks like the obvious label and is wrong
for every rain-affected match, where the chase wins on a reduced target and
therefore reads as a batting-first win. Measured on cricsheet ODI male
2014-2026, which carries a trustworthy \code{outcome_method}:\tabular{llll}{
    \tab matches \tab true bf rate \tab score-derived bf rate \cr
   normal \tab 1,143 \tab 0.4829 \tab 0.4838 \cr
   D/L \tab 108 \tab 0.4815 \tab \strong{0.8148} \cr
}


D/L is 8.6\% of matches and the derived label is 33 points wrong on them,
which was enough to make a correctly-calibrated model look badly
miscalibrated (ODI male chase ECE 0.1064 against a true 0.0781).
}

\section{Do not use \code{winner_team_id} either}{

That column names a team which is not in the match for most rows — 71.7\% of
T20, 56.5\% of ODI, and every Hundred match. Match \code{1513717} is India U19
(\code{1803}) against UAE U19 (\code{3675}) with \code{winner_team_id} \code{1854}.
}

