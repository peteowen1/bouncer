% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_schema.R
\name{create_elo_calibration_metrics_table}
\alias{create_elo_calibration_metrics_table}
\title{Create the ELO calibration metrics table}
\usage{
create_elo_calibration_metrics_table(conn)
}
\arguments{
\item{conn}{A DuckDB connection object}
}
\value{
Invisibly returns TRUE
}
\description{
The ONE declaration of this table. It had four: \code{create_schema()}, the
calibration-compute path, the \verb{01_calibrate_expected_values.R} pipeline step
and the test fixtures -- and they had already drifted. The compute path
declared \code{calculated_date} as VARCHAR with no primary key while the schema
declared DATE with \verb{PRIMARY KEY (format, metric_type, metric_key)}. Both are
\verb{CREATE TABLE IF NOT EXISTS}, so whichever ran first on a given database
silently won and nothing ever complained.
}
\details{
This is the same failure as the 3-way ELO table name (#63): two declarations
of one truth drift, and the drift is invisible because each is individually
correct.

Callers must be able to run this on a database that has never had the full
schema applied -- recalibrating should not be gated on a step that has
nothing to do with calibration.
}
\keyword{internal}
