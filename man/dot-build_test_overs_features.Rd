% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_overs_model.R
\name{.build_test_overs_features}
\alias{.build_test_overs_features}
\title{Build the per-ball feature frame for the Test overs model}
\usage{
.build_test_overs_features(conn, match_type = c("Test", "MDM"))
}
\arguments{
\item{conn}{DBI connection.}

\item{match_type}{Character. "Test" or "MDM".}
}
\value{
A data.table, one row per delivery, ready for .test_overs_formula().
}
\description{
Pulls every completed Test/MDM delivery (male, any innings <= 4,
wickets_before clipped to 0-9 since the 10th wicket ends the innings) and
derives every pre-ball state column from the row's OWN outcomes -- verified
empirically that total_runs/wickets_fallen in cricsheet.deliveries are
POST-ball inclusive, so pre-ball state is never an adjacent-row LAG.
}
\keyword{internal}
