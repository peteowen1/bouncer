% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{team_skills_for_role}
\alias{team_skills_for_role}
\title{Pick a team's skills for one role}
\usage{
team_skills_for_role(skills, role)
}
\arguments{
\item{skills}{List. Flat or role-specific team skills.}

\item{role}{Character. "batting" or "bowling".}
}
\value{
The list of skills for \code{role}.
}
\description{
Accepts either the flat \code{list(runs_skill=, wicket_skill=)} shape or the
role-specific \code{list(batting=, bowling=)} shape, and returns the values that
apply to \code{role}. See \code{\link[=simulate_match_ballbyball]{simulate_match_ballbyball()}}'s "Team skill roles".
}
\details{
Deliberately NOT \code{skills$batting \%||\% skills}. \code{$} partial-matches on lists,
so on a flat list whose only "batting"-prefixed element were, say,
\code{batting_team_runs_skill}, \code{skills$batting} would return that bare NUMBER
rather than NULL -- and the simulator would silently run an innings with a
scalar where it expected a list of team skills. Today two such fields exist,
making the match ambiguous and the result NULL by luck rather than by
design; one rename would turn that luck into a silent wrong answer.
}
\keyword{internal}
