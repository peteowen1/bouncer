% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rating_validation.R
\name{rating_reliability}
\alias{rating_reliability}
\title{Reliability of a Per-Observation Rating Input}
\usage{
rating_reliability(value, player, min_obs = 2L)
}
\arguments{
\item{value}{Numeric vector, one element per observation (e.g. per innings).}

\item{player}{Grouping vector of the same length identifying the player.}

\item{min_obs}{Integer. Players with fewer observations than this are dropped
before the decomposition. Default 2, the minimum for a within-player
variance to exist at all.}
}
\value{
A list with \code{within_sd}, \code{between_sd} (sampling-error corrected),
\code{icc} (single-observation reliability), \code{n_players}, \code{n_obs},
\code{mean_obs_per_player}, \code{reliability} (of a mean over
\code{mean_obs_per_player}), and \code{obs_for(target)}, the number of observations
needed to reach a given reliability. \code{between_sd} is floored at zero: a
negative variance estimate means the data cannot distinguish players at all.
}
\description{
One-way random-effects decomposition (ICC(1)) of a per-innings quantity into
between-player signal and within-player noise, plus the Spearman-Brown
reliability of a player mean over \code{n} observations.
}
\section{Why this is here}{

A leaderboard built from a noisy per-innings statistic orders sampling error,
not players, and nothing in the output looks wrong when it does. Measured on
\code{batting_era} (the term that dominates EPR) in 2026-08-13:\tabular{llll}{
    \tab within sd \tab between sd \tab reliability at n \cr
   T20 batting \tab 14.16 \tab 2.03 \tab 0.403 at n=33 \cr
   ODI batting \tab 27.86 \tab 4.93 \tab 0.467 at n=28 \cr
}


Roughly half of the observed spread between players was sampling error, which
is why the T20 leaderboard put Shreyas Iyer first on a mean of 7.66 with a
median of -0.39. Run this before trusting, tuning against, or publishing any
ranking built from a per-innings value.
}

